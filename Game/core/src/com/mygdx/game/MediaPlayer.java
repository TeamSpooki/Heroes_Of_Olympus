package com.mygdx.game;

import java.awt.BorderLayout;
import java.awt.Component;
import java.net.URL;

import javax.swing.JFrame;

public class MediaPlayer extends JFrame {
	/*
	 * public MediaPlayer(String URL){ setLayout(new BorderLayout()); //file you
	 * want to play URL mediaURL = new URL(URL); //create the media player with the
	 * media url //Player mediaPlayer = Manager.createRealizedPlayer(mediaURL);
	 * //get components for video and playback controls Component video =
	 * mediaPlayer.getVisualComponent(); Component controls =
	 * mediaPlayer.getControlPanelComponent(); add(video,BorderLayout.CENTER);
	 * add(controls,BorderLayout.SOUTH); }
	 */
}
